/*
 * Copyright 2015 Contactless Devices, LLC.
 *
 * The code contained herein is licensed under the GNU General Public
 * License. You may obtain a copy of the GNU General Public License
 * Version 2 or later at the following locations:
 *
 * http://www.opensource.org/licenses/gpl-license.html
 * http://www.gnu.org/copyleft/gpl.html
 */

/ {
	description = "WBE-I-W1: 1-Wire interface module";
	compatible-slots = "wb5-mod";
	compatible = "contactless,imx28-wirenboard52";

	fragment@0 {
		target = <&MOD_DT_ALIAS(pins)>;

		__overlay__ {
			fsl,pinmux-ids = <
				MOD_PINMUX_GPIO(SDA)
				MOD_PINMUX_GPIO(SCL)
			>;
			fsl,drive-strength = <MXS_DRIVE_8mA>;
			fsl,voltage = <MXS_VOLTAGE_HIGH>;
			fsl,pull-up = <MXS_PULL_DISABLE>;
		};
	};

	fragment@1 {
		target-path = "/";

		__overlay__ {
			MOD_DT_ALIAS(onewire): MOD_DT_ALIAS(onewire)@0 {
				compatible = "w1-gpio";
				gpios = <MOD_GPIO(SDA)>;
				pu-gpios = <MOD_GPIO(SCL)>;
			};
		};
	};
};
