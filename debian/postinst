#!/bin/bash
set -e

. /usr/lib/wb-utils/wb_env.sh
wb_source "of"

. /usr/lib/wb-hwconf-manager/functions.sh

BOARD_CONFFILE=/var/lib/wb-hwconf-manager/system.conf

is_live_system && {
    BOARD_CONFFILE_DIST=`get_dist_conffile`
	cat "$BOARD_CONFFILE_DIST" > $BOARD_CONFFILE
}

#DEBHELPER#
